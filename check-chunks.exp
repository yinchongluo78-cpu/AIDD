#!/usr/bin/expect -f

set timeout 30

spawn ssh root@120.24.22.244
expect "password:"
send "Lyc001286\r"

expect "root@*"
send "cd /var/www/lyc-ai/api\r"

expect "root@*"
send "npx prisma db execute --stdin <<< \"SELECT doc_id, COUNT(*) as chunk_count FROM kb_chunks GROUP BY doc_id;\"\r"

sleep 3

expect "root@*"
send "exit\r"

expect eof
