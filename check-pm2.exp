#!/usr/bin/expect -f
set timeout 30
set password "Lyc001286"

spawn ssh root@120.24.22.244
expect {
    "password:" {
        send "$password\r"
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
}

expect "# "
send "pm2 status\r"
expect "# "
send "pm2 logs --lines 10\r"
expect "# "
send "exit\r"
expect eof