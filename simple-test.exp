#!/usr/bin/expect -f
set timeout 10
set password "Lyc001286"

spawn ssh root@120.24.22.244
expect {
    "password:" {
        send "$password\r"
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
}

expect "# "
send "curl -v http://localhost/api/health\r"
expect "# "
send "curl -v http://localhost/api/conversations\r"
expect "# "
send "exit\r"
expect eof