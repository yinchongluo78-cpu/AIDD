#!/usr/bin/expect -f
set timeout 30
set password "Lyc001286"

spawn ssh root@120.24.22.244
expect {
    "password:" {
        send "$password\r"
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
}

expect "# "
send "cd /var/www/lyc-ai/api\r"
expect "# "
send "cat prisma/schema.prisma | head -20\r"
expect "# "
send "ls -la prisma/\r"
expect "# "
send "exit\r"
expect eof