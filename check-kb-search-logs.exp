#!/usr/bin/expect -f
set timeout 30
set password "Lyc001286"

spawn ssh root@120.24.22.244
expect {
    "password:" {
        send "$password\r"
    }
}

expect "# "

puts "\n查看知识库检索相关的日志..."
send "pm2 logs lyc-ai-api --lines 100 --nostream | grep -A 10 -B 5 \"知识库检索\\|searchDocument\\|relevantChunks\"\r"
expect "# "
sleep 3

send "exit\r"
expect eof
